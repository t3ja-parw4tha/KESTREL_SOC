# NOTE: `docker build -f docker/Dockerfile .` uses the repo root as context.
# Only the root `.dockerignore` is applied. Keep that file authoritative.

.venv
venv
__pycache__
*.pyc
.env
.git
.github
*.db
.mypy_cache
.pytest_cache
htmlcov
.coverage
